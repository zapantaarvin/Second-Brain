# Reflections — grilling

Append-only history of skill updates driven by real usage. See the `reflecting-on-skills` Skill for the process. Don't overwrite prior entries.

## 2026-08-20 — Study Mode gets its own visual identity, separate from frontend-design

**Observed**: Built the first real Study Mode output (`HW1 Foundations`, covering 4 prerequisite ML concepts) using `frontend-design`'s confirmed warm-beige/Urbanist identity, with static illustrations and a deliberately-withheld full derivation (academic integrity caution). Arvin pointed to a prior artifact he'd already made and confirmed taught him better: a single-question deep-dive (`HW1 Q1 — Agreement Probability Proof`) using a completely different technical/monospace style, with a full step-by-step derivation and a live interactive slider demo.

**Confidence**: High — direct comparison against Arvin's own prior work, which he explicitly confirmed as more effective, not an inferred guess.

**Applied**: Yes. Added a "Visual presentation for Study Mode" section to `grilling`'s `SKILL.md`, extracting the exact confirmed tokens (monospace type, blue/green/red semantic palette, dual light+dark theme with a manual toggle, numbered step-card derivations, interactive elements over static ones) from the real HTML source of Arvin's prior artifact. Deliberately kept separate from `frontend-design`'s identity — confirmed explicitly that study/math content and consumer-app content warrant different visual registers, not one universal system.

**Also corrected**: the academic-integrity caution from the first `HW1 Foundations` attempt was overcautious — withholding the full derivation mechanism wasn't necessary; showing *how* a proof is built is teaching, not producing the graded submission text. Full worked derivations are now the explicit default for Study Mode.

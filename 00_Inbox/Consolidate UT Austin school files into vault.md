---
type: inbox
captured: 2026-08-20
status: pending
---

# Consolidate UT Austin school files into vault

Arvin has a school folder living outside `Documents/Brain` (path not yet given) that should move into `School/30_Resources/UT-Austin/` so all UT Austin material lives in one place instead of split across two locations.

## Open questions before this can be executed
- Exact path of the external folder — need this from Arvin.
- What's actually in it (raw course files: slides, PDFs, assignments?) vs. what's already in `UT-Austin/` (7 distilled reference notes + `_index.md`) — are these duplicates, or does the external folder hold the primary material the vault notes were distilled from?
- Whether to literally move (external folder disappears) or copy (external folder stays as the working copy, vault holds curated notes) — `School/CLAUDE.md` already says vault notes are "distilled reference notes only... full course materials live outside this vault," so a straight move might contradict that existing convention. Worth resolving before acting, not assuming.

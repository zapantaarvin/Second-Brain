# Obsidian local config — contains plugin API keys and private keys, never commit
.obsidian/

# MCP server config — embeds the Local REST API bearer key, never commit
.mcp.json

# Personal/Finance content belongs in the private Second-Brain-Personal repo, never here.
# These files were physically moved out on 2026-08-16 (not just gitignored) -- these
# lines are a defensive backstop in case anything with these exact names ever gets
# recreated here by mistake, under any category folder (updated to filename-only
# patterns 2026-08-19 since the old flat-PARA paths no longer exist post-reorg).
**/Dating Playbook.md
**/Finance Dashboard.md
**/Stock portfolio task.md

.DS_Store

# CLAUDE.md — General

Loaded automatically once Claude reads or edits a file inside `General/`. See the root `CLAUDE.md` for vault-wide rules (domain boundary, do-not-touch list, Skills) — this file only adds what's specific to this category.

## What belongs here
Real reference content or cross-cutting project/area work that doesn't cleanly fit `School/`, `Projects/`, or `Business/`. Used deliberately, not as a dumping ground for anything uncertain — that's what `00_Inbox` (vault root) is for at capture time. If in doubt whether something is General vs. a specific category, ask rather than defaulting here.

## Current contents
- `30_Resources/AI Brain Skills/AI Brain Skills Strategy.md` — the Skills-system taxonomy, build order, and trigger-test log pointer. Lives here because it documents the vault's own tooling, not a business/school/app-project topic.
